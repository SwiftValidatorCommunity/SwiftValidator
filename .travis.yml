language: objective-c
osx_image: xcode7.1

before_install:
  - gem install cocoapods --no-rdoc --no-ri --no-document --quiet
  - gem install xcpretty --no-rdoc --no-ri --no-document --quiet

script:
  - xcodebuild clean build test -project Validator.xcodeproj -scheme Validator -sdk iphonesimulator9.2
  - pod lib lint